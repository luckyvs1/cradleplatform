# Turn off automatic schema creation; use schema.sql instead
spring.jpa.hibernate.ddl-auto=none

# Show sql statements in console:
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true