/**
 * File: users.js
 * Summary:
 *  Handles signing up for users.
 *  May remove this later, as we will want only admins to be able to create user accounts.
 */

import api from "../api";
import { userLoggedIn } from "./auth";

export const signup = data => dispatch => console.log("update information (signup)");

